import { GameConfig } from "../../types/game";

export const CATCH_FRUITS_CONFIG: GameConfig = {
  energyCost: 10,
  minDuration: 30,
  maxScorePerSecond: 8,
  pgcPerPoint: 0.5,
  expPerPoint: 0.1,
  chestDropChance: 0.7,
};
